<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :url_yt %><br>
    <%= f.text_field :url_yt %>
  </div>
  <div class="field">
    <%= f.label :id_project %><br>
    <%= f.number_field :id_project %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
